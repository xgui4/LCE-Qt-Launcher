name: Build and Deploy Nightly

on:
  push:
    branches:
      - nightly

permissions:
     contents: write

jobs:
  nightly:
    name: Build Nightly
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .
          pip install nuitka

      - name: Build with Nuitka for Linux
        if: matrix.os == 'ubuntu-latest' 
        run: |
          python -m nuitka --onefile --standalone --enable-plugin=pyside6 --output-dir=dist src/main.py

      - name: Build with Nuitka for Windows
        if: matrix.os == 'ubuntu-latest' 
        run: |
          python -m nuitka --enable-plugin=pyside6 --output-dir=dist src/main.py

      - name: Package Windows build
        if: matrix.os == 'windows-latest'
        run: |
          powershell Compress-Archive -Path dist/* -DestinationPath LCE-Qt-Launcher-Windows.zip

      - name: Package Linux build
        if: matrix.os == 'ubuntu-latest'
        run: |
          tar -czf LCE-WQt-Launcher-Linux.tar.gz dist/*

      - name: Upload Nightly
        uses: andelf/nightly-release@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: nightly
          name: Nightly Release
          body: |
            Experimental nightly builds for Windows and Linux.
          files: |
            *.zip
            *.tar.gz